import { defineConfig } from 'vite';

export default defineConfig({
  // The service is intentionally separate from the main RoVoCar PWA but uses
  // the same GitHub Pages account.
  base: '/rovocar-csv-maker/',
});

